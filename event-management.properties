msg = Event management Properties DeRefreshhhhhhhhhhhhhhhhhhhhhh
## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.h2.console.enabled=true
spring.datasource.url=jdbc:h2:file:./data/repository/eventmgmt
spring.jpa.hibernate.ddl-auto=update
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driverClassName=org.h2.Driver
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

## QuartzProperties
spring.quartz.job-store-type = jdbc
spring.quartz.jdbc.initialize-schema=always

## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB


logging.level.org.hibernate.SQL=error
logging.config=classpath:logback-spring.xml
logging.pattern.console= "%d{yy	yy-MM-dd HH:mm:ss} - %msg%n"
logging.pattern.file="%d{yyyy-MM-dd HH:mm:ss} - %msg%n"



#configs to connect to a secured server
spring.boot.admin.client.url=http://localhost:8090
spring.boot.admin.client.username=securestats
spring.boot.admin.client.password=infa@securestats

management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always

logging.file.name=/root/eventmngmnt/logs/log.log
management.endpoint.logfile.external-file=/root/eventmngmnt/logs/log.log
